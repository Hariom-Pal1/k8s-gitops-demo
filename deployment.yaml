apiVersion:app/v1
kind: deployment
metadata:
  name: nginx
spec:
replicas: 2
selector: 
  matchlables:
  app: nginx
template:
  metadata:
    lables:
    app: nginx
spec:
containers:
- name: nginx
  image: nginx
- ports:
   - containerports: 80

